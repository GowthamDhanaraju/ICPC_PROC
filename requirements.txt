# ============================================================
# Batch Video Proctoring Pipeline — Python Dependencies
# Pin versions for reproducible server deployments.
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0

# --- Data Validation ---
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Database ---
sqlalchemy==2.0.31
alembic==1.13.2

# --- Computer Vision ---
opencv-python-headless>=4.10.0
numpy>=1.26.4
# --- ML Frameworks (optional — set MOCK_ML_MODELS=True to skip) ---
ultralytics>=8.2.54
mediapipe>=0.10.30
onnxruntime>=1.18.1
imageio-ffmpeg>=0.5.1

# --- Face Detection & Identity (InsightFace SCRFD + ArcFace) ---
# SCRFD achieves 92.8 mAP on WiderFace Hard; ArcFace 99.5% on LFW
insightface>=0.7.3

# --- Gaze / Head Pose (6DRepNet via PyTorch) ---
# Also needed for Silero VAD torch backend
torch>=2.0.0
torchvision>=0.15.0

# --- Audio Transcription (faster-whisper — 4× faster than openai-whisper) ---
faster-whisper>=1.0.0

# --- Speaker Diarization (resemblyzer GE2E — no HuggingFace token required) ---
resemblyzer>=0.1.3
soundfile>=0.12.1

# --- Optional: pyannote speaker diarization (best accuracy, needs HF token) ---
# Uncomment to enable DIARIZATION_BACKEND=pyannote
# pyannote.audio>=3.1.0

scipy>=1.13.1

# --- AWS ---
boto3==1.34.144
botocore==1.34.144

# --- Celery / Redis Worker Queue ---
celery[redis]==5.4.0
redis==5.0.7

# --- Google Drive downloader (for test-gdrive CLI mode) ---
gdown==5.2.0

# --- HTTP Client ---
requests==2.32.3

# --- Testing ---
pytest==8.2.2
pytest-env==1.1.3
httpx==0.27.0
